/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: "/rpl.rubenportfolio/",
  basePath: "/rpl.rubenportfolio",
  trailingSlash: true,
  output: "export",
};

export default nextConfig;
